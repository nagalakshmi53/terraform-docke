# Terraform Docker Nginx Task

This project demonstrates how to use **Terraform** to provision a simple **Docker container** running **Nginx** on port 8080.

## 🧱 What This Project Does

- Uses the Docker provider in Terraform
- Pulls the official Nginx image
- Creates a container mapped to port 8080
- Shows the Nginx welcome page on [http://localhost:8080](http://localhost:8080)

## 📂 Project Structure

